//Object.entries

const data = {
    frontend: `Yarod`,
    backend: `Kevin`,
    design: `Leelo`
}

const entries = Object.entries(data); //Es necesario tener la O mayus.

console.log(entries); //Lo que hará Object.entries es pasar los objetos a un array, tanto sus elementos como sus keys


