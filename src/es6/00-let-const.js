//Anteriormente ocupabamos "var" para declarar variables, desde esta version la reemplazan let y const

var a = a; //Funcionan desde la parte global
let b = b; //Fuincionan desde el bloque que se asigna
const c = c; //Fuincionan desde el bloque que se asigna